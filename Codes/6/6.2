#include <iostream>
#include <string.h>

using namespace std;

void zavd2(char Arr2[70]);

int main()
{
    setlocale(0, "ukr");

    char Arr2[70];

    cout << "Введiть ПIБ: ";
    cin.getline(Arr2, 70);
    zavd2(Arr2);
    cout << "Довжина: " << strlen(Arr2) << '\n';
    cout << "\nDirect by Hokage" << endl;
}
void zavd1(char Arr1[70])
{

    int i, space = 0, n_char, l_sur = 0;
    int n = strlen(Arr2), kil_a = 0;

    for (i = 0; Arr2[i] != '\0'; i++)
    {
        if (Arr2 [i] == ' ')
        {
            space++;
        }
        if (space == 2)
        {
            n_char = i;
            space++;
        }
        if (space == 0)
        {
            l_sur++;
        }
        if (tolower(Arr2[i]) == 'a')
        {
            kil_a++;
        }
    }
    cout << "Кiлькiсть букв 'а' у масивi: " << kil_a << endl;
}
